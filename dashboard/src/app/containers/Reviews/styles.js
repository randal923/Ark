import styled from 'styled-components';

const Container = styled.div`
	margin: 2rem;
	span {
		cursor: pointer;
		color: var(--blue);
		font-size: 1.4rem;
	}

	h2 {
		margin-top: 1rem;
	}
`;

export { Container };
